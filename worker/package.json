{
  "name": "worker",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -b",
    "start": "node --env-file-if-exists=./../.env ./dist/main.js"
  },
  "dependencies": {
    "fluent-ffmpeg": "^2.1.3"
  }
}
